/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import { ECommerceProgram } from "./ECommerceProgram";
import { ECommerceProgramCountArgs } from "./ECommerceProgramCountArgs";
import { ECommerceProgramFindManyArgs } from "./ECommerceProgramFindManyArgs";
import { ECommerceProgramFindUniqueArgs } from "./ECommerceProgramFindUniqueArgs";
import { DeleteECommerceProgramArgs } from "./DeleteECommerceProgramArgs";
import { ECommerceProgramService } from "../eCommerceProgram.service";
@graphql.Resolver(() => ECommerceProgram)
export class ECommerceProgramResolverBase {
  constructor(protected readonly service: ECommerceProgramService) {}

  async _eCommerceProgramsMeta(
    @graphql.Args() args: ECommerceProgramCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @graphql.Query(() => [ECommerceProgram])
  async eCommercePrograms(
    @graphql.Args() args: ECommerceProgramFindManyArgs
  ): Promise<ECommerceProgram[]> {
    return this.service.eCommercePrograms(args);
  }

  @graphql.Query(() => ECommerceProgram, { nullable: true })
  async eCommerceProgram(
    @graphql.Args() args: ECommerceProgramFindUniqueArgs
  ): Promise<ECommerceProgram | null> {
    const result = await this.service.eCommerceProgram(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @graphql.Mutation(() => ECommerceProgram)
  async deleteECommerceProgram(
    @graphql.Args() args: DeleteECommerceProgramArgs
  ): Promise<ECommerceProgram | null> {
    try {
      return await this.service.deleteECommerceProgram(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }
}
